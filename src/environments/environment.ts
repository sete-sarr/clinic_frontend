export const environment = {
  production: true,
  // Replace with the real Render backend URL before deploying (exemple_prod.md §3), e.g.
  // 'https://clinic-backend.onrender.com/api/v1'. A relative path only works if the frontend and
  // backend share one origin, which isn't the case with Vercel + Render as separate domains.
  apiBaseUrl: 'https://clinic-backend-p0km.onrender.com/api/v1',
};
